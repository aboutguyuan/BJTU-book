# 今日工作复盘与学习笔记

日期：2026-07-06

项目：`aboutguyuan/BJTU-book`

仓库地址：<https://github.com/aboutguyuan/BJTU-book>

当前可访问地址：

- Vercel：<https://bjtu-book.vercel.app>
- GitHub Pages：<https://aboutguyuan.github.io/BJTU-book/>
- 七牛 Kodo 空间：`bjtu-book`

## 1. 今天完成了什么

今天围绕《北京交通大学生存手册》做了完整的静态网站发布链路，从本地前端项目、GitHub、Vercel、七牛云 Kodo 到 GitHub Pages 都走了一遍。

主要成果：

- 把 Markdown 手册整理成可浏览的前端文档站。
- 完成章节拆分和网页目录导航。
- 完成首页封面和作者 GitHub 头像展示。
- 调整了封面标题字号和头像尺寸。
- 部署到 Vercel。
- 尝试部署到七牛云 Kodo，并上传了静态文件。
- 排查了七牛测试域名、私有空间、默认首页、备案域名等问题。
- 配置了 GitHub Pages，并最终让网站上线。
- 增加了自动化构建和发布脚本，后续更新内容后可以自动发布。

## 2. 本地项目做了哪些事

项目是一个无框架静态站，核心文件是：

| 文件 | 作用 |
| --- | --- |
| `index.html` | 页面结构，包含顶部导航、封面、目录、正文容器 |
| `style.css` | 整个网站视觉风格，复古工程教材 / 蓝图纸 / 技术文档站风格 |
| `app.js` | 前端交互逻辑，负责章节渲染、目录高亮、搜索、移动端抽屉 |
| `data.js` | 构建后生成的手册数据，浏览器通过它加载正文 |
| `content/chapters/` | 每章单独的 Markdown 文件，是正文的主要维护入口 |
| `tools/build.mjs` | 构建脚本，把 Markdown 转成 `data.js` 和 `dist/` |
| `tools/split-chapters.mjs` | 从完整 Markdown 拆分章节 |
| `tools/kodo-config.mjs` | 生成七牛 Kodo 上传配置 |
| `.github/workflows/pages.yml` | GitHub Pages 自动部署 workflow |
| `vercel.json` | Vercel 部署配置 |

本地构建命令：

```bash
npm run build
```

构建后会生成：

```text
dist/
  index.html
  style.css
  app.js
  data.js
  .nojekyll
```

`dist/` 就是最终可以上传到各种静态托管平台的文件夹。

## 3. 网站前端学到了什么

这个项目是典型的静态内容站。你可以把它理解成：

```text
Markdown 原稿
    ↓
tools/build.mjs 构建
    ↓
data.js 结构化数据
    ↓
app.js 在浏览器渲染
    ↓
用户看到网页
```

关键概念：

- `HTML` 负责页面骨架。
- `CSS` 负责视觉风格和响应式布局。
- `JavaScript` 负责把数据渲染到页面上。
- `Markdown` 是内容源。
- `build` 是把内容源转换成网页可直接读取的数据。
- `dist/` 是部署产物，不是主要编辑入口。

以后想改文章内容，优先改：

```text
content/chapters/
```

改完运行：

```bash
npm run build
```

## 4. Vercel 部署学到了什么

Vercel 的特点：

- 对前端项目非常友好。
- 连上 GitHub 后，push 到 `main` 会自动部署。
- 免费额度足够个人静态站使用。
- 默认提供 `vercel.app` 域名。
- 不需要备案。

本项目 Vercel 地址：

```text
https://bjtu-book.vercel.app
```

Vercel 适合：

- 个人项目展示。
- 面向海外或不强依赖大陆访问稳定性的用户。
- 快速预览和发布前端项目。

Vercel 的问题：

- 中国大陆访问不一定稳定。
- 有时需要网络环境支持。
- 如果要面向大陆用户正式发布，国内 CDN + 备案域名更稳。

## 5. 七牛云 Kodo 部署学到了什么

七牛云 Kodo 是对象存储，适合存放静态文件。

今天做了：

- 安装 Qshell。
- 配置七牛账号。
- 创建 Kodo 空间 `bjtu-book`。
- 构建项目。
- 上传 `dist/` 里的静态文件到 Kodo。

上传命令：

```bash
QINIU_BUCKET=bjtu-book npm run kodo:deploy
```

实际上传成功的文件：

```text
index.html
style.css
app.js
data.js
```

后来也补充了 `.nojekyll`，这是给 GitHub Pages 用的；七牛不依赖它。

七牛 Kodo 相关脚本：

```bash
npm run kodo:config
npm run kodo:deploy
```

学到的重点：

- Kodo 空间名就是 Bucket 名。
- 静态网站默认首页通常填 `index.html`。
- 如果空间是私有的，普通 URL 会返回 `401 Unauthorized`。
- 带 token 的 URL 是临时私有链接，不适合作为正式网站链接。
- 七牛测试域名可能限制 HTML 访问，不适合作为正式网站域名。
- 正式给大陆用户访问，需要绑定自己的已备案域名。

## 6. 关于备案和国内托管

国内稳定访问通常绕不开 ICP 备案。

基本流程：

```text
买域名
  ↓
域名实名认证
  ↓
购买备案接入资源
  ↓
提交 ICP 备案
  ↓
管局审核
  ↓
备案通过
  ↓
绑定七牛 / 阿里云 / 腾讯云 CDN
```

备案本身一般不收费，但会产生这些成本：

- 域名费用：大约几十元/年。
- 备案接入资源：大约几十到几百元/年。
- CDN / 存储流量：低访问量时很低，访问量上来后按量计费。

你今天理解到的核心差异：

- Vercel、GitHub Pages 这类海外平台更像开发者托管平台，免费层是增长策略。
- 国内 OSS、COS、Kodo 更像云基础设施，默认是资源计费。
- 国内正式公开访问还涉及备案、域名、CDN、内容合规和带宽成本。

## 7. GitHub Pages 部署学到了什么

GitHub Pages 是 GitHub 提供的免费静态网站托管服务。

本项目最终地址：

```text
https://aboutguyuan.github.io/BJTU-book/
```

今天做了：

- 新增 `.github/workflows/pages.yml`。
- 构建项目。
- 把 `dist/` 发布到 `gh-pages` 分支。
- 验证首页、样式文件和数据文件可以访问。

最终采用的方式：

```text
main 分支
  ↓ push
GitHub Actions
  ↓ npm run build
dist/
  ↓
gh-pages 分支
  ↓
GitHub Pages
```

为什么没有用 GitHub Actions 官方 Pages 模式：

- 第一次尝试时，workflow 没有权限自动创建 Pages site。
- 报错是 `Resource not accessible by integration`。
- 所以改成更传统、更稳的 `gh-pages` 分支发布方式。

现在的自动化逻辑：

- 你以后 push 到 `main`。
- GitHub Actions 会自动运行 `npm run build`。
- 构建结果会自动更新到 `gh-pages`。
- GitHub Pages 会从 `gh-pages` 发布网站。

## 8. 今天遇到的问题和对应理解

### 1. `zsh: parse error near '<'`

原因：命令里的 `<AccessKey>`、`<SecretKey>` 是占位符，实际输入时不能带尖括号。

错误写法：

```bash
qshell account <AccessKey> <SecretKey> aboutguyuan
```

正确写法：

```bash
qshell account AccessKey SecretKey aboutguyuan
```

### 2. 七牛返回 `401 Unauthorized`

原因：空间是私有空间，公开 URL 没有权限访问。

解决：

- 把 Kodo 空间改成公开空间。
- 或者使用带 token 的私有链接，但这不适合做公开网站。

### 3. 七牛测试域名返回 `403 Forbidden`

原因：七牛测试域名不适合正式发布 HTML 网站，可能限制 HTML 访问。

解决：

- 绑定自己的已备案域名。
- 配置 CNAME 到七牛 CDN。

### 4. GitHub Pages workflow 失败

原因：GitHub Actions 默认 token 没有权限首次创建 Pages site。

解决：

- 改用 `gh-pages` 分支发布。
- 让 workflow 只负责构建并更新 `gh-pages`。

### 5. GitHub Pages 需要 `.nojekyll`

原因：GitHub Pages 默认可能按 Jekyll 规则处理静态文件。

解决：

- 在 `dist/` 中生成 `.nojekyll`。
- 本项目已在 `tools/build.mjs` 里自动生成。

## 9. 以后如何更新网站

日常更新文章：

```bash
# 1. 修改章节内容
content/chapters/

# 2. 本地构建检查
npm run build

# 3. 提交
git add content/chapters data.js
git commit -m "Update guide content"

# 4. 推送
git push origin main
```

推送后：

- Vercel 会自动部署。
- GitHub Actions 会自动更新 GitHub Pages。
- 如果要更新七牛，需要手动执行：

```bash
QINIU_BUCKET=bjtu-book npm run kodo:deploy
```

## 10. 现在已有的发布渠道

| 渠道 | 地址 / 状态 | 特点 |
| --- | --- | --- |
| GitHub 仓库 | <https://github.com/aboutguyuan/BJTU-book> | 源码和内容管理 |
| Vercel | <https://bjtu-book.vercel.app> | 自动部署，访问不一定适合大陆 |
| GitHub Pages | <https://aboutguyuan.github.io/BJTU-book/> | 免费静态托管，已上线 |
| 七牛 Kodo | `bjtu-book` 空间 | 文件已上传，正式访问需要备案域名 |

## 11. 安全提醒

今天你曾经把七牛云的 AccessKey / SecretKey 发到聊天中。

建议：

1. 去七牛云控制台。
2. 打开密钥管理。
3. 禁用或删除旧密钥。
4. 重新生成新密钥。
5. 本机重新执行：

```bash
qshell account 新AccessKey 新SecretKey aboutguyuan
```

以后不要把密钥发到聊天、截图、GitHub、Markdown 或任何公开页面里。

## 12. 你可以继续学习的方向

### 前端基础

- HTML：页面结构。
- CSS：布局、响应式、视觉风格。
- JavaScript：数据渲染和交互。

### 静态站构建

- 为什么要有 `dist/`。
- 为什么源码和部署产物要分开。
- Markdown 如何转成网页数据。

### Git 和 GitHub

- `main` 分支用于源码。
- `gh-pages` 分支用于发布产物。
- GitHub Actions 用于自动化构建。

### 部署平台

- Vercel：现代前端部署。
- GitHub Pages：免费静态托管。
- 七牛 Kodo：国内对象存储。
- CDN：让静态文件访问更快、更稳定。

### 国内上线

- 域名。
- 实名认证。
- ICP 备案。
- CNAME。
- CDN 加速域名。

## 13. 当前最实用的结论

短期最推荐使用：

```text
GitHub Pages + Vercel
```

原因：

- 免费。
- 已经可访问。
- 自动部署。
- 对这个纯静态项目足够。

如果未来要正式面向大陆用户长期稳定访问，再走：

```text
备案域名 + 七牛 Kodo + 七牛 CDN
```

这条路更正式，但需要成本和备案时间。

