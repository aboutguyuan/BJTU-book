# BJTU-book 项目使用与内容更新指南

这份文档用于后续维护《北京交通大学生存手册》网页。现在项目以 GitHub Pages 作为主要线上发布方式，七牛云 Kodo 可作为国内静态文件镜像。

## 一、最常用更新流程

如果只是修改正文内容，按这个顺序做：

```bash
# 1. 修改 content/chapters/ 里的章节 Markdown
npm run build
npm run preview
git status
git add content/chapters data.js
git commit -m "Update guide content"
git push origin main
```

推送到 `main` 后，GitHub Actions 会自动构建并发布到 GitHub Pages：

```text
https://aboutguyuan.github.io/BJTU-book/
```

如果还要同步到七牛云 Kodo，先确认七牛账号已配置，然后执行：

```bash
QINIU_BUCKET=bjtu-book npm run kodo:deploy
```

## 二、文件和网页显示区域对应关系

| 文件 / 目录 | 前端显示或维护内容 | 日常是否常改 |
| --- | --- | --- |
| `content/chapters/` | 网站正文来源。每个 Markdown 文件对应一个章节，左侧目录、正文、搜索数据都来自这里。 | 最常改 |
| `index.html` | 页面骨架：首页封面、顶部导航、作者信息、邮箱、GitHub 链接、搜索框、正文容器。 | 偶尔改 |
| `style.css` | 全站视觉样式：字体、字号、行距、封面标题、三栏布局、移动端适配。 | 偶尔改 |
| `app.js` | 前端交互：渲染章节、目录高亮、搜索、移动端目录抽屉、复制链接。 | 少改 |
| `tools/build.mjs` | 构建脚本：把 Markdown 解析成 `data.js` 和 `dist/`。 | 很少改 |
| `tools/split-chapters.mjs` | 把完整 Markdown 稿拆成逐章文件。 | 只在重新拆章时用 |
| `tools/serve.mjs` | 本地预览服务器。 | 不需要改 |
| `tools/kodo-config.mjs` | 七牛 Kodo 上传配置生成脚本。 | 只在七牛部署规则变化时改 |
| `data.js` | 构建生成的数据，浏览器读取它来显示章节。 | 不建议手改 |
| `dist/` | 构建产物，上传到静态托管平台用。 | 不手改 |
| `.github/workflows/pages.yml` | GitHub Pages 自动部署流程。 | 少改 |
| `package.json` | 项目命令入口，例如 `npm run build`、`npm run preview`、`npm run kodo:deploy`。 | 少改 |

## 三、正文内容怎么维护

正文维护入口是：

```text
content/chapters/
```

章节顺序由文件名前面的编号决定，例如：

```text
00-preface.md
01-xxx.md
02-xxx.md
```

每个章节文件建议保持这个结构：

```md
## 第一章 章节标题

章节开头正文。

### 1.1 小节标题

小节正文。
```

规则：

- `##` 会被识别为一级章节，并出现在左侧目录。
- `###` 会被识别为小节，并出现在右侧 “ON THIS PAGE”。
- 文件名前缀编号控制章节排序。
- 改完正文后必须运行 `npm run build`，否则网页数据不会更新。

## 四、首页内容怎么改

首页封面在 `index.html` 的 `.manual-cover` 区域。

常见对应关系：

| 想改的内容 | 修改位置 |
| --- | --- |
| 顶部标题 `BJTU / SURVIVAL MANUAL` | `index.html` 的 `.brand` |
| 顶部导航 `CONTENTS / AUTHOR / GITHUB` | `index.html` 的 `.header-nav` |
| 作者头像 | `index.html` 里 `https://github.com/aboutguyuan.png?size=80` |
| 封面英文标题 | `index.html` 的 `.title-en` |
| 封面中文标题 | `index.html` 的 `.title-cn` |
| 封面说明文字 | `index.html` 的 `.cover-summary` 和 `.cover-note` |
| GitHub 个人主页 | 搜索 `https://github.com/aboutguyuan` |
| GitHub 项目链接 | 搜索 `https://github.com/aboutguyuan/BJTU-book` |
| 邮箱展示 | `index.html` 的 `.meta-grid` 里的 `EMAIL` 项 |

当前邮箱展示为：

```text
aboutguyuan@163.com（勘误和投稿）
```

## 五、样式怎么改

主要改 `style.css`。

| 想改的样式 | CSS 选择器 |
| --- | --- |
| 全站颜色 | `:root` 里的 `--accent`、`--ink` 等变量 |
| 顶部导航 | `.site-header`、`.brand`、`.header-nav` |
| 左侧目录 | `.lesson-sidebar`、`.chapter-link` |
| 封面英文标题大小 | `.title-en` |
| 封面中文标题大小和行距 | `.title-cn` |
| 封面说明文字 | `.cover-summary`、`.cover-note` |
| 作者头像小徽章 | `.author-chip`、`.author-chip img` |
| 首页信息格 | `.meta-grid` |
| 正文章节标题 | `.chapter h2` |
| 正文小节标题 | `.chapter h3` |
| 手机端样式 | `@media (max-width: 900px)` |

改样式后运行：

```bash
npm run build
npm run preview
```

## 六、不要手动改的内容

- 不要手动改 `dist/`，它会被 `npm run build` 覆盖。
- 不要手动改 `data.js`，除非你明确知道构建输出结构。
- 不要把未确认公开的 DOCX、PDF、草稿文件加入 Git。
- 不要提交七牛云 AccessKey、SecretKey、`.env` 或任何私密 token。

## 七、发布渠道

当前推荐：

| 渠道 | 用途 | 地址 / 说明 |
| --- | --- | --- |
| GitHub 仓库 | 源码和内容管理 | `https://github.com/aboutguyuan/BJTU-book` |
| GitHub Pages | 主要公开访问地址 | `https://aboutguyuan.github.io/BJTU-book/` |
| 七牛云 Kodo | 国内静态文件镜像 | 空间名 `bjtu-book`，正式域名需要备案 |

## 八、常见问题

### 改了文字但网页没变

通常是没有重新构建。运行：

```bash
npm run build
```

### 左侧目录顺序不对

检查 `content/chapters/` 文件名前缀编号。

### 右侧目录缺少小节

检查小节标题是否使用 `###`。

### GitHub Pages 没更新

先检查本地是否已经提交并推送：

```bash
git status
git log --oneline -5
```

再到 GitHub 仓库的 Actions 页面查看 Pages workflow 是否运行成功。

### 七牛云访问不是首页

确认已经上传 `dist/index.html`，并在七牛空间或绑定域名处配置默认首页为 `index.html`。
