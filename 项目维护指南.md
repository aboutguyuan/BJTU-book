# BJTU-book 项目维护指南

这份文档用于说明本地项目文件和网站前端显示区域之间的对应关系，方便后续增删查改、重新构建、推送 GitHub 和部署 Vercel。

## 一、项目整体结构

当前项目是一个无框架静态网站，不使用 React/Vue/Next。核心流程是：

```text
Markdown 章节文件
  -> tools/build.mjs 解析生成 data.js
  -> index.html + style.css + app.js 读取 data.js
  -> 浏览器渲染为文档站
  -> npm run build 生成 dist/
  -> Vercel 部署 dist/
```

主要文件结构：

```text
BJTU-book/
├── index.html                  # 页面骨架：顶部导航、封面、侧边栏、正文容器、搜索框
├── style.css                   # 全站样式：封面、三栏布局、字体、按钮、移动端适配
├── app.js                      # 前端交互：渲染章节、搜索、目录高亮、移动端抽屉
├── data.js                     # 构建生成的数据文件，供根目录 index.html 直接读取
├── package.json                # npm 脚本入口
├── vercel.json                 # Vercel 构建和路由配置
├── content/
│   └── chapters/               # 网站实际优先读取的逐章 Markdown 内容
├── tools/
│   ├── build.mjs               # 把 Markdown 解析成 data.js 和 dist/
│   ├── split-chapters.mjs      # 把完整稿拆成 content/chapters/ 下的逐章文件
│   └── serve.mjs               # 本地静态预览服务器
├── dist/                       # npm run build 生成的部署产物，不提交
└── 北京交通大学生存指南/
    └── 北京交通大学生存手册_重构完整稿.md   # 原始完整稿，拆章来源
```

## 二、文件和网页区域对应关系

### `index.html`

负责页面的静态骨架。它决定页面上有哪些主要区域，但不直接写入所有章节正文。

对应网页区域：

| 文件位置 | 网页区域 | 作用 |
|---|---|---|
| `.site-header` | 顶部固定导航栏 | 显示 `BJTU / SURVIVAL MANUAL`、`CONTENTS`、`AUTHOR`、`GITHUB`、搜索按钮、复制按钮 |
| `.lesson-sidebar` | 左侧章节目录 | `app.js` 会把 `data.js` 中的章节标题填进 `#chapterNav` |
| `.mobile-drawer` | 移动端目录抽屉 | 手机端点击菜单按钮后显示章节列表 |
| `.manual-cover` | 首页封面 | 大标题、作者 GitHub 小头像、项目按钮、搜索框 |
| `#manualContent` | 正文区域 | `app.js` 会把所有章节正文渲染到这里 |
| `.page-toc` | 右侧当前页目录 | 滚动时显示当前章节的小节目录 |
| `<script src="./data.js">` | 数据入口 | 加载构建后的手册数据 |
| `<script src="./app.js">` | 交互入口 | 执行渲染和交互逻辑 |

常见修改：

- 修改顶部导航文案：改 `index.html` 里的 `.site-header`。
- 修改封面标题、副标题、按钮：改 `.manual-cover` 里的内容。
- 修改作者 GitHub 头像：当前使用 `https://github.com/aboutguyuan.png?size=80`。
- 修改 GitHub 主页或项目链接：搜索 `github.com/aboutguyuan` 和 `aboutguyuan/BJTU-book`。

### `style.css`

负责所有视觉样式。

对应网页区域：

| CSS 选择器 | 控制内容 |
|---|---|
| `:root` | 全局颜色、字体变量 |
| `body` | 页面纸张底色、点阵背景、正文基础字体 |
| `.site-header` | 顶部导航栏样式 |
| `.lesson-sidebar` | 左侧章节目录样式 |
| `.manual-cover` | 首页封面整体高度、边线、间距 |
| `.title-en` | 封面英文标题 `BJTU SURVIVAL` 字号 |
| `.title-cn` | 封面中文标题字号 |
| `.author-chip` | 作者小头像徽章 |
| `.cover-button` | 封面按钮 |
| `.meta-grid` | AUTHOR / PROJECT / SOURCE 信息格 |
| `.search-box` | 搜索框 |
| `.chapter h2` | 每章标题样式 |
| `.chapter h3` | 每节标题样式 |
| `.page-toc` | 右侧当前页目录 |
| `@media (max-width: 900px)` | 手机端适配 |

常见修改：

- 封面标题太大或太小：改 `.title-en` 和 `.title-cn` 的 `font-size`。
- 头像太大或太小：改 `.author-chip img` 的 `width` 和 `height`。
- 页面主色：改 `--accent`。
- 正文宽度：改 `.lesson-content` 的 `max-width`。
- 左侧目录宽度：改 `.lesson-sidebar` 的 `width`，同时调整 `.lesson-main` 的 `margin-left`。

### `app.js`

负责浏览器端动态渲染和交互。

主要职责：

| 函数/逻辑 | 作用 |
|---|---|
| `const data = window.SITE_DATA` | 读取 `data.js` 暴露的数据 |
| `renderChapterLinks()` | 渲染左侧章节目录和移动端目录 |
| `renderContent()` | 把章节正文渲染到 `#manualContent` |
| `setActiveChapter()` | 滚动时高亮当前章节，并更新右侧 TOC |
| `setActiveToc()` | 高亮右侧当前小节 |
| `runSearch()` | 搜索章节、小节和正文 |
| `copyCurrentLink()` | 复制当前章节链接 |
| `openDrawer()` / `closeDrawer()` | 控制移动端目录抽屉 |
| `IntersectionObserver` | 监听滚动位置，自动切换高亮 |

一般情况下，增删章节不需要改 `app.js`。只有新增交互功能时才需要修改它。

### `content/chapters/`

这是网站内容维护最重要的目录。网站构建时会优先读取这里的 Markdown 文件。

文件命名规则：

```text
00-写在前面-这不是一本让你舒服的手册.md
01-第一章-认识北交大-标签-现实和边界.md
02-第二章-入学第一学期-先活明白.md
...
27-第二十七章-给家长和准考生的话.md
90-A-附录-A-新生第一周清单.md
...
99-结语-不要把选择外包.md
```

排序规则：

- `tools/build.mjs` 会按文件名排序读取。
- 所以文件名前面的编号非常重要。
- 新增章节时，建议使用两位数字编号，例如 `28-第二十八章-xxx.md`。
- 新增附录时，建议使用 `90-H-附录-H-xxx.md`。

每个章节文件内部格式：

```md
## 第X章 章节标题

章节开头正文。

### X.1 小节标题

小节正文。

### X.2 小节标题

小节正文。
```

注意：

- 一级网站标题 `# 北京交通大学生存手册` 不需要写进每个章节文件。
- 每个章节文件应该从 `##` 开始。
- 小节使用 `###`。
- 如果小节没有使用 `###`，右侧 TOC 不会显示它。

### `北京交通大学生存指南/北京交通大学生存手册_重构完整稿.md`

这是原始完整稿。它主要用于批量拆章。

当前构建逻辑是：

1. 如果 `content/chapters/` 里有 Markdown 文件，优先读取逐章文件。
2. 如果 `content/chapters/` 不存在或为空，才读取完整稿。

因此，日常维护建议直接改 `content/chapters/`，不要同时改完整稿和逐章文件，否则容易出现内容不一致。

如果你确实想从完整稿重新生成所有章节：

```bash
npm run chapters
npm run build
```

警告：`npm run chapters` 会删除并重建 `content/chapters/`，会覆盖你在逐章文件里手动改过但没有同步回完整稿的内容。

### `data.js`

这是构建生成的数据文件，供根目录 `index.html` 直接读取。

作用：

- 让你直接打开项目根目录页面或用根目录预览时，不再出现“请先运行 npm run build”。
- 存放章节数组、标题、正文 HTML、搜索文本等数据。

不要手动编辑 `data.js`。修改内容后运行：

```bash
npm run build
```

### `dist/`

这是部署产物目录，由 `npm run build` 自动生成。

包含：

```text
dist/index.html
dist/style.css
dist/app.js
dist/data.js
```

注意：

- `dist/` 已被 `.gitignore` 忽略。
- Vercel 部署时会运行 `npm run build`，然后发布 `dist/`。
- 不要手动修改 `dist/`，因为下次构建会覆盖。

### `tools/build.mjs`

构建脚本。它负责：

1. 读取 `content/chapters/` 下的逐章 Markdown。
2. 如果逐章文件不存在，则回退读取完整稿。
3. 解析 Markdown。
4. 生成章节结构、正文 HTML、搜索文本。
5. 写入根目录 `data.js`。
6. 生成 `dist/` 部署产物。

支持的 Markdown 能力：

- `##` 章节标题
- `###` 小节标题
- 段落
- 有序列表
- 无序列表
- 引用块
- 表格
- 代码块
- 行内代码
- 链接
- `<!-- pagebreak -->`

如果未来要支持图片、脚注、复杂 Markdown，可以扩展这个文件。

### `tools/split-chapters.mjs`

拆章脚本。它负责把完整稿按 `##` 标题拆成多个文件，输出到 `content/chapters/`。

使用场景：

- 你重新整理了一份完整稿，希望批量生成逐章文件。
- 你第一次从单文件稿件迁移到逐章维护。

使用命令：

```bash
npm run chapters
```

注意：这个命令会重建 `content/chapters/`。

### `tools/serve.mjs`

本地静态预览服务器。

常用命令：

```bash
npm run preview
```

默认预览 `dist/` 目录。

如果要预览根目录页面：

```bash
node tools/serve.mjs .
```

### `vercel.json`

Vercel 部署配置。

当前配置含义：

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

说明：

- Vercel 构建命令：`npm run build`
- 部署目录：`dist`
- 不使用前端框架预设
- 所有路径都回退到 `index.html`，避免刷新带 hash 或路径时出现 404

## 三、常用维护操作

### 修改某一章内容

1. 打开 `content/chapters/`。
2. 找到对应章节 Markdown 文件。
3. 修改正文。
4. 运行：

```bash
npm run build
```

5. 本地预览：

```bash
npm run preview
```

6. 提交并推送：

```bash
git add content/chapters data.js
git commit -m "Update chapter content"
git push
```

### 新增一章

1. 在 `content/chapters/` 新建文件，例如：

```text
28-第二十八章-校园资源地图.md
```

2. 文件内容从 `##` 开始：

```md
## 第二十八章 校园资源地图

这里写章节开头。

### 28.1 图书馆

这里写小节内容。
```

3. 运行：

```bash
npm run build
```

4. 检查左侧目录是否出现新章节。

### 删除一章

1. 删除 `content/chapters/` 中对应文件。
2. 运行：

```bash
npm run build
```

3. 检查左侧目录和搜索结果。

### 调整章节顺序

修改文件名前面的排序编号。

例如：

```text
06-第六章-绩点-排名和能力-不要用一个指标理解自己.md
```

改成：

```text
05-第六章-绩点-排名和能力-不要用一个指标理解自己.md
```

然后运行：

```bash
npm run build
```

注意：排序由文件名决定，不由正文里的标题决定。

### 修改章节标题

需要同步改两处：

1. 文件名，方便排序和识别。
2. 文件第一行 `## 章节标题`，这是网页真正显示的标题。

然后运行：

```bash
npm run build
```

### 修改首页封面

主要改两个文件：

- `index.html`：改封面文案、按钮、作者链接。
- `style.css`：改封面样式、字号、间距。

常见位置：

| 想改的内容 | 修改位置 |
|---|---|
| 英文标题 `BJTU SURVIVAL` | `index.html` 的 `.title-en` |
| 中文标题 | `index.html` 的 `.title-cn` |
| 标题大小 | `style.css` 的 `.title-en`、`.title-cn` |
| 作者头像 | `index.html` 的 `.author-chip img src` |
| 头像显示大小 | `style.css` 的 `.author-chip img` |
| 封面按钮 | `index.html` 的 `.cover-actions` |
| 封面说明文字 | `index.html` 的 `.cover-summary` 和 `.cover-note` |

### 修改网站颜色和字体

改 `style.css` 顶部的 `:root`：

```css
:root {
  --bg: #fafaf5;
  --surface: #f3f1e8;
  --ink: #1a1a1a;
  --accent: #3553ff;
  --font-body: "Source Serif 4", Georgia, serif;
  --font-display: "VT323", ui-monospace, monospace;
  --font-mono: "JetBrains Mono", ui-monospace, Consolas, monospace;
}
```

最常改的是：

- `--accent`：蓝色强调色
- `--bg`：纸张底色
- `--ink`：正文颜色

### 修改搜索逻辑

搜索逻辑在 `app.js` 的 `runSearch()`。

当前搜索范围：

- 章节标题
- 章节正文
- 小节标题
- 小节正文

当前最多显示 12 条结果：

```js
results.slice(0, 12)
```

如果想显示更多结果，修改这个数字。

## 四、构建、预览、部署

### 安装依赖

这个项目没有第三方运行依赖，但仍建议保留 `package-lock.json`。

```bash
npm install
```

### 构建

```bash
npm run build
```

成功后会看到类似：

```text
Built 36 chapters into dist/
```

构建会更新：

- `data.js`
- `dist/index.html`
- `dist/style.css`
- `dist/app.js`
- `dist/data.js`

### 本地预览

预览部署产物：

```bash
npm run preview
```

访问：

```text
http://127.0.0.1:4173
```

### 推送 GitHub

```bash
git status
git add <需要提交的文件>
git commit -m "Your commit message"
git push
```

当前远端仓库：

```text
https://github.com/aboutguyuan/BJTU-book
```

### Vercel 部署

当前 Vercel 已连接 GitHub 仓库。只要 push 到 `main`，Vercel 会自动部署。

线上地址：

```text
https://bjtu-book.vercel.app
```

如果需要手动部署：

```bash
npx vercel@latest --prod
```

## 五、不要随便提交的文件

当前本地有一些未跟踪文件，不属于网站当前线上内容：

```text
LICENSE.docx
北京交通大学生存指南/AAA上海交通大学生存手册.pdf
北京交通大学生存指南/北京交通大学生存手册_扩写版.md
北京交通大学生存指南/北京交通大学生存手册_扩写版_75页排版稿.md
北京交通大学生存指南/北京交通大学生存指南_重构写作大纲.md
北京交通大学生存指南/语言风格promot.docx
```

如果只是维护网站，不要把这些文件加入 Git，除非你明确想公开它们。

提交前务必看：

```bash
git status
```

只提交你确认需要公开和部署的内容。

## 六、常见问题

### 页面显示“请先运行 npm run build”

原因：

- 页面没有加载到 `data.js`。
- 或者你打开的是根目录页面，但没有生成根目录 `data.js`。

解决：

```bash
npm run build
```

### 改了 Markdown 但网页没变化

可能原因：

- 没有运行 `npm run build`。
- 改的是完整稿，但构建优先读取 `content/chapters/`。
- 改了 `dist/`，但 `dist/` 会被重新构建覆盖。

建议：

日常直接改 `content/chapters/`，然后运行：

```bash
npm run build
```

### 左侧目录顺序不对

原因：

- 文件名前缀编号不对。

解决：

修改 `content/chapters/` 文件名前面的编号，然后重新构建。

### 右侧 TOC 没有显示某个小节

原因：

- 小节标题没有使用 `###`。

解决：

把小节标题写成：

```md
### 1.1 小节标题
```

### Vercel 页面国内打不开或很慢

Vercel 在中国大陆直连不稳定。如果主要读者在国内，建议另做国内镜像，例如：

- 阿里云 OSS + CDN
- 腾讯云 COS + CDN
- 七牛云
- 又拍云
- 国内服务器 + Nginx

## 七、推荐的日常工作流

### 小改某一章

```bash
# 1. 修改 content/chapters/ 中对应章节
npm run build
npm run preview
git status
git add content/chapters data.js
git commit -m "Update chapter content"
git push
```

### 改样式

```bash
# 1. 修改 style.css
npm run build
npm run preview
git status
git add style.css
git commit -m "Update site style"
git push
```

### 改首页封面

```bash
# 1. 修改 index.html 和 style.css
npm run build
npm run preview
git status
git add index.html style.css
git commit -m "Update homepage cover"
git push
```

### 从完整稿重新拆章

```bash
# 先确认完整稿是最新版本
npm run chapters
npm run build
npm run preview
git status
git add content/chapters data.js
git commit -m "Regenerate chapter files"
git push
```

注意：这会覆盖已有逐章文件。

## 八、维护原则

1. 内容优先改 `content/chapters/`。
2. 页面结构改 `index.html`。
3. 视觉样式改 `style.css`。
4. 交互逻辑改 `app.js`。
5. 构建规则改 `tools/build.mjs`。
6. 拆章规则改 `tools/split-chapters.mjs`。
7. 每次发布前运行 `npm run build`。
8. 每次提交前运行 `git status`，避免误传本地资料。
9. 不要手动改 `dist/`。
10. 不要手动改 `data.js`，除非你明确知道它是生成文件。
